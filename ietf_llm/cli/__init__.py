"""Command-line surfaces.

The auxiliary `ietf-llm-*` console scripts (`cli.export`, `cli.search`) and the
corpus-listing helpers the `ietf-llm` gather CLI uses (`cli.list`). The main
`ietf-llm` entry point itself lives in `ietf_llm.__main__`.
"""
